jupyter-book<1.0
pre-commit
# Uncomment if using mermaid diagrams
# sphinxcontrib.mermaid

# Needed to execute the Python computer exercises in book/chapter*.ipynb.
# This file is what the GitHub Pages workflow installs, so every dependency
# the exercises import at build time belongs here. The live-code (Thebe) and
# Binder buttons do NOT use this file -- binder/requirements.txt takes
# precedence for them, and deliberately omits pyldpc.
numpy
scipy
matplotlib
pyldpc          # chapter13: LDPC encoding/decoding
